if ! git pull; then
  echo "Error: git pull falló. Por favor, resuelve los conflictos antes de continuar."
  exit 1
fi

# Continuar con el commit si git pull fue exitoso
echo "git pull completado con éxito. Continuando con el commit..."